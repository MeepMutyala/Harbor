# Harbor Rust Bridge

Minimal Rust bridge for local LLM and filesystem access.

## Responsibilities
- Expose JSON-RPC over localhost (`/rpc`).
- Route LLM calls through `any-llm-rust` (planned).
- Provide scoped filesystem access.

## Run (dev)
```
cargo run
```
