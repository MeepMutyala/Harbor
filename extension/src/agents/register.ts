export function registerAgentsAPI(): void {
  // TODO: Inject Web Agents API into pages via content script.
  // For now, keep a visible marker that the background loaded.
  console.log('[Harbor] Agents API register placeholder');
}
