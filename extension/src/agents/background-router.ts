/**
 * Background Router
 * 
 * Routes messages from content scripts to appropriate handlers.
 * This is a minimal stub - the main message handlers are in background.ts
 */

export function initializeRouter(): void {
  console.log('[Harbor] Router initialized');
}
