[package]
name = "my-mcp-server"
version = "1.0.0"
edition = "2021"
description = "A WASM MCP server"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Optimize for small WASM size
[profile.release]
opt-level = "s"
lto = true
strip = true
