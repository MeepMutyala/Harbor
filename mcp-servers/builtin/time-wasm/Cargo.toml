[package]
name = "mcp-time-wasm"
version = "1.0.0"
edition = "2021"
description = "A simple MCP server that returns the current time"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
opt-level = "s"
lto = true
strip = true
